// const express = require('express');
// const router = express.Router();
// const paddleController = require('../controllers/paddleController');

// // router.post('/webhook', express.raw({ type: 'application/json' }), paddleController.handleWebhook);
// router.post('/get-checkout-details', paddleController.getCheckoutDetails);

// module.exports = router;
